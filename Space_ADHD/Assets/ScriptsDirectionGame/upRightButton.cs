using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class upRightButton : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        this.transform.position = new Vector3(Screen.width / 2, Screen.height / 6, 0);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
